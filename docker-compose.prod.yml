services:
  # --- Base de Datos (TimescaleDB) ---
  db:
    image: timescale/timescaledb:latest-pg16
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - wealthroad_postgres_data:/var/lib/postgresql/data
    networks:
      - wealth-net

  # --- Backend (FastAPI) ---
  backend:
    build: ./backend
    restart: always
    expose:
      - "8000" # Solo visible para Caddy, no para internet directo
    environment:
      DATABASE_URL: ${DATABASE_URL}
    depends_on:
      - db
    networks:
      - wealth-net

  # --- Servidor Web (Caddy) ---
  caddy:
    image: caddy:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy_data:/data
      - caddy_config:/config
      - ./Caddyfile:/etc/caddy/Caddyfile
    networks:
      - wealth-net

volumes:
  wealthroad_postgres_data:
  caddy_data:
  caddy_config:

networks:
  wealth-net:
    driver: bridge
